name: e_garrison
description: Track guards.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  form_field_validator: ^1.1.0
  flutter_signin_button: ^2.0.0
  flash: ^2.0.3
  cupertino_icons: ^1.0.2
  google_fonts: ^3.0.1
  freezed_annotation: ^2.1.0
  firebase_core: ^1.10.0
  firebase_auth: ^3.2.0
  google_sign_in: ^5.2.1
  dartz: ^0.10.0
  flutter_riverpod: ^1.0.0
  auto_route: ^5.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  freezed: ^2.1.0+1
  build_runner: ^2.1.5
  auto_route_generator: ^5.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/
